import { {{pascalCase name}} } from "lib";
import { StoryAnchor, ImportSnippet, Story, Meta, ArgsTable } from "stories/common";

import { API } from "./API.story";

export const title = "{{storyPath}}/{{pascalCase name}}";
export const componentName = "{{pascalCase name}}";

<Meta title={title} component={ {{pascalCase name}} } anchor />

<PageTitle title="{{pascalCase name}}" subtitle="{{pascalCase name}} description here" />

<StoryAnchor name="API" path={title} />

## API

#### Import

<ImportSnippet componentName={componentName} />

#### Playground

<Story name="API" story={API} />

<ArgsTable Component={ {{pascalCase name}} } name="API" />