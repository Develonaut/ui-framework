import { Meta, ColorPalette, ColorItem } from "@storybook/addon-docs/blocks";
import { Story } from "stories/components";
import { Canvas, SourceState } from "@storybook/addon-docs/blocks";
import { ThemeProvider } from "lib";
import { paramCase, capitalCase } from "change-case";
import { STORIES } from "stories/config";

import commonColorsExport from "stories/stylesheets/exports/common-colors.module.scss";
import graysExport from "stories/stylesheets/exports/grays.module.scss";
import paletteColorsExport from "stories/stylesheets/exports/palette-colors.module.scss";
import textColorsExport from "stories/stylesheets/exports/text-colors.module.scss";
import actionColorsExport from "stories/stylesheets/exports/action-colors.module.scss";

export const config = STORIES.COLORS;

<Meta title={config.title} />

export const ColorSwatches = ({
  colors,
  titleFormatter,
  subtitleFormatter,
}) => (
  <ColorPalette>
    {Object.entries(colors).map(([key, value]) => (
      <ColorItem
        title={typeof titleFormatter === "function" ? titleFormatter(key) : key}
        subtitle={
          typeof subtitleFormatter === "function" ? subtitleFormatter(key) : key
        }
        colors={[value]}
      />
    ))}
  </ColorPalette>
);

# Colors

#### Colors help convey meaning through the use of color in our applications. Out of the box these are UI-Frameworks brand colors.

<br />

## Palette

Palette colors are used to service a given intention within an application. The UI-Framework exposes the following palette colors (accessible using the `main.scss` or `_variables.scss`):

- Primary - used to represent primary interface elements for a user. It's the color displayed most frequently across your app's screens and components.
- Secondary - used to represent secondary interface elements for a user. It provides more ways to accent and distinguish your product. Having it is optional.
- Error - used to represent interface elements that the user should be made aware of.
- Warning - used to represent potentially dangerous actions or important messages.
- Info - used to present information to the user that is neutral and not necessarily important.
- Success - used to indicate the successful completion of an action that user triggered.

<br />

<Story name="Palette" disableCanvas>
  <ColorPalette>
    <ColorItem
      title="Primary"
      subtitle="$colors-primary-(light|main|dark)"
      colors={{
        light: paletteColorsExport.primaryLight,
        main: paletteColorsExport.primaryMain,
        dark: paletteColorsExport.primaryDark,
      }}
    />
    <ColorItem
      title="Error"
      subtitle="$colors-error-(light|main|dark)"
      colors={{
        light: paletteColorsExport.errorLight,
        main: paletteColorsExport.errorMain,
        dark: paletteColorsExport.errorDark,
      }}
    />
    <ColorItem
      title="Success"
      subtitle="$colors-success-(light|main|dark)"
      colors={{
        light: paletteColorsExport.successLight,
        main: paletteColorsExport.successMain,
        dark: paletteColorsExport.successDark,
      }}
    />
    <ColorItem
      title="Default"
      subtitle="$colors-default-(light|main|dark)"
      colors={{
        light: paletteColorsExport.defaultLight,
        main: paletteColorsExport.defaultMain,
        dark: paletteColorsExport.defaultDark,
      }}
    />
  </ColorPalette>
</Story>

## Common

<Story name="Common" disableCanvas>
  <ColorSwatches
    colors={commonColorsExport}
    titleFormatter={(val) => capitalCase(val)}
    subtitleFormatter={(val) => `$colors-common-${val}`}
  />
</Story>

## Grays

<Story name="Grays" disableCanvas>
  <ColorSwatches
    colors={graysExport}
    titleFormatter={(val) => capitalCase(val)}
    subtitleFormatter={(val) => `$colors-grays-${val}`}
  />
</Story>

## Text

<Story name="Text" disableCanvas>
  <ColorSwatches
    colors={textColorsExport}
    titleFormatter={(val) => capitalCase(val)}
    subtitleFormatter={(val) => `$colors-text-${val}`}
  />
</Story>

## Actions

<Story name="Actions" disableCanvas>
  <ColorSwatches
    colors={actionColorsExport}
    titleFormatter={(val) => capitalCase(val)}
    subtitleFormatter={(val) => `$colors-action-${val}`}
  />
</Story>
