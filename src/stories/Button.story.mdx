import {
  Meta,
  Story,
  Canvas,
  ArgsTable,
  SourceState,
  Source,
  Anchor,
  Title,
  Heading,
  Subheading,
} from "@storybook/addon-docs/blocks";
import { Button } from "lib/components";
import { StoryAnchor, ImportSnippet } from "stories/components";
import { getPackageName, getImportSnippet } from "stories/utils";
import { paths } from "stories/config";

export const componentName = "Button";
export const storyPath = paths.button;

<Meta title={storyPath} component={Button} anchor />

# Button

#### Buttons allow users to take actions, and make choices, with a single tap.

Buttons communicate actions that users can take. They are typically placed throughout your UI, in places like:

- Dialogs
- Modal windows
- Forms
- Cards
- Toolbars

## Contained Buttons

Contained buttons are high-emphasis, distinguished by their use of elevation and fill. They contain actions that are primary to your app.

<Canvas isExpanded withSource={SourceState.OPEN}>
  <Story
    name="Contained"
    parameters={{
      layout: "centered",
    }}
  >
    <Button>Default</Button>
    <Button color="primary">Primary</Button>
    <Button disabled>Disabled</Button>
    <Button href="#contained-buttons" color="primary">
      Link
    </Button>
  </Story>
</Canvas>

<StoryAnchor name="Text" path={storyPath} />

## Text Buttons

Text buttons are typically used for less-pronounced actions, including those located:

- In dialogs
- In cards

In cards, text buttons help maintain an emphasis on card content.

<Canvas isExpanded withSource={SourceState.OPEN}>
  <Story
    name="Text"
    parameters={{
      layout: "centered",
    }}
  >
    <Button variant="text">Default</Button>
    <Button variant="text" color="primary">
      Primary
    </Button>
    <Button variant="text" disabled>
      Disabled
    </Button>
    <Button variant="text" color="primary" href="#text-buttons">
      Link
    </Button>
  </Story>
</Canvas>

<StoryAnchor name="API" path={storyPath} />

## API

#### Import

<ImportSnippet componentName={componentName} />

#### Playground

<Canvas>
  <Story
    name="API"
    args={{ children: "Default" }}
    parameters={{ layout: "centered" }}
  >
    {(args) => <Button {...args} />}
  </Story>
</Canvas>

<ArgsTable of={Button} story="API" />
